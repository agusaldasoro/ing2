.title-bar
  = link_to bar.name, bar_path(bar)
.data-bar
  .item-data-bar
    .item-data-bar-name
      | Ubicación
    .item-data-bar-data
      = bar.address.value
  .item-data-bar
    .item-data-bar-name
      | Latitud
    .item-data-bar-data
      = bar.address.lat
  .item-data-bar
    .item-data-bar-name
      | Longitud
    .item-data-bar-data
      = bar.address.lng
  .item-data-bar
    .item-data-bar-name
      | Tiene Wifi
    .item-data-bar-data
      = t("#{bar.has_wifi.value}Value")
.data-bar
  - bar.characteristics.each do |c|
    - unless c.type == 'Address' || c.type == 'HasWifi'
      .item-data-bar
        .item-data-bar-name
          = t("#{c.type}")
        .item-data-bar-data
          = c.value
